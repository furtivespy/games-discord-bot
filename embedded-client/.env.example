# Public Discord application id (same as the bot). Safe to expose to the page.
VITE_CLIENT_ID=

# Host only, used by patchUrlMappings in local Vite. Production injects this
# from PUBLIC_BASE_URL when Express serves index.html.
VITE_API_HOST=localhost:5173
